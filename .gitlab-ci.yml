image : node:alpine

stages :
  - build
  - test

build :
  stage: build 
  script :
    - npm ci --cache .npm --prefer-offline
  cache:
    key: $NODE_MODULES
    paths: 
      - .npm/


test:
  stage: test
  script: 
    - npm test


